export interface TodoListType {
    id: string;
    title: string | null;
    body: string | null;
    created_at: string;
  }

export interface TodoType {
  
    title: string | null;
    body: string | null;
  }